<div class="form-check">
    <input class="form-check-input" type="radio" name="base-topic" value="{{uuid}}" id="radio-{{uuid}}">
    <label class="form-check-label" for="radio-{{uuid}}">
        {{description}}
    </label>
</div>