<div class="form-check">
    <input class="form-check-input" type="radio" name="radio-{{uuid}}" id="topics-radio-{{uuid}}">
    <label class="form-check-label" for="topics-radio-{{uuid}}">
        {{description}}
    </label>
</div>