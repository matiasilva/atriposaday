<h1>{{title}}</h1>
<p>Make changes to the overall functioning of this <em>A Tripos a Day</em> deployment.</p>

<div class="row">
    <div class="col">
        <div class="card">
            <div class="card-header">
                Upload a new question
            </div>
            <div class="card-body">
                <form method="POST" action="/admin/create/question">
                    <div class="mb-3">
                        <label for="exampleInputEmail1" class="form-label">Description</label>
                        <input type="email" class="form-control" id="exampleInputEmail1" aria-describedby="emailHelp">
                        <div id="emailHelp" class="form-text">Optional description. Batch uploads will all have the same
                            description.</div>
                    </div>
                    <div class="mb-3">
                        <select class="form-select" aria-label="Default select example">
                            <option selected>Tripos part</option>
                            <option value="1">IA</option>
                            <option value="2">IB</option>
                            <option value="3">IIA</option>
                            <option value="4">IIB</option>
                        </select>
                    </div>
                    <div class="mb-3">
                        <label class="form-label">Base topic</label>
                        {{#each root_topics}}
                        {{> admin/checkbox description=this.name uuid=this.uuid}}
                        {{/each}}
                        <div id="topicHelp" class="form-text">All questions must belong to a base topic. If none
                            applies, choose the <em>no topic</em> option.</div>
                    </div>
                    <div class="mb-3">
                        <label for="formFileMultiple" class="form-label">Upload one or more photos</label>
                        <input class="form-control form-control-lg" type="file" id="formFileMultiple" multiple>
                    </div>
                    <button id="question-create-submit" type="submit" class="btn btn-primary">Submit</button>
                </form>

            </div>
        </div>
    </div>
    <div class="col">
        <div class="card">
            <div class="card-header">
                Create a new topic
            </div>
            <div class="card-body">
                <form>
                    <div class="mb-3">
                        <label for="exampleInputEmail1" class="form-label">Email address</label>
                        <input type="email" class="form-control" id="exampleInputEmail1" aria-describedby="emailHelp">
                        <div id="emailHelp" class="form-text">We'll never share your email with anyone else.</div>
                    </div>
                    <div class="mb-3">
                        <label for="exampleInputPassword1" class="form-label">Password</label>
                        <input type="password" class="form-control" id="exampleInputPassword1">
                    </div>
                    <div class="mb-3 form-check">
                        <input type="checkbox" class="form-check-input" id="exampleCheck1">
                        <label class="form-check-label" for="exampleCheck1">Check me out</label>
                    </div>
                    <button type="submit" class="btn btn-primary">Submit</button>
                </form>
            </div>
        </div>
    </div>
</div>